language: c
os:
  - linux
before_install:
  - git clone https://github.com/jedisct1/libsodium
  - cd libsodium
  - ./autogen.sh
  - ./configure
  - make
  - sudo make install
  - cd ..
  - ./autogen.sh
  - git clone https://github.com/tlinden/p5mode tests/lib
compiler:
  - clang
  - gcc
script:
 - ./configure
 - make
 - make check
 - make test

